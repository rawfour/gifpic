import React from 'react';
import { storiesOf } from '@storybook/react';
import SectionTitle from '../components/SectionTitle';

storiesOf('Section title', module).add('Section title', () => (
  <SectionTitle>Lorem ipsum</SectionTitle>
));
